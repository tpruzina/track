TODO (H - high, M - medium, L - low)

H
  - make separate init command for data(path,base) initialization, don't do it
    automatically
  - rm - doesn't yet phisically remove files, only stops tracking them
  - snapshot
  - GC - TODO

M - implement export of snapshots into tar format (tgz), or via pipe

L - protect against possible sql injections via crafted files - DONE
